import { MixerPanning } from '../../parameters/mixer/panning'
import { MixerVolume } from '../../parameters/mixer/volume'

export default {
    name: 'Mixer',
    parameters: [MixerVolume, MixerPanning],
}
